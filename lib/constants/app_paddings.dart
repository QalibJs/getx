import 'package:flutter/material.dart';

class AppPaddings {
  AppPaddings._();

  static EdgeInsets get lrt12 => const EdgeInsets.only(
        left: 12,
        right: 12,
        top: 12,
      );
  static EdgeInsets get all4 => const EdgeInsets.all(4);
}
